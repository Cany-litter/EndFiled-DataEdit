server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/endfiled?useUnicode=true&characterEncoding=utf8mb4
    username: root
    password: root
  redis:
    host: localhost
    port: 6379

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.endfiled.model
