server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/endfiled?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: ${DB_PASSWORD:1234}
  redis:
    host: localhost
    port: 6379

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.endfiled.model
